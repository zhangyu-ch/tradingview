# 项目结构总览

> 面向对象：第一次接触项目的新用户、想二次开发的同学  
> 阅读目标：快速搞清楚项目主干、常见功能入口、以及 `charting-library` 如何抽到别的项目里复用

## 1. 项目是做什么的？

这是一个以 Python 为核心的缠论分析工具，覆盖这些能力：

- 行情接入：A 股、港股、美股、期货、外汇、数字货币等
- 缠论计算：分型、笔、线段、中枢、走势段、背驰、买卖点
- Web 展示：基于 TradingView `charting-library` 的图表页面
- 策略与回测：策略编写、历史回测、参数优化
- 实盘与量化接入：掘金、vn.py、wtpy 等

如果你把这个仓库看成一套系统，可以粗分成三层：

1. `src/chanlun`：核心计算与业务主线
2. `web/chanlun_chart`：Web 页面和图表服务
3. `script` / `notebook` / `joinquant`：运行脚本、实验示例、外部平台样例

## 2. 顶层目录怎么理解？

| 目录/文件 | 作用 | 是否核心 |
| --- | --- | --- |
| `src/` | 核心 Python 代码，都在这里 | 是 |
| `web/` | Web 图表页面与服务入口 | 是 |
| `script/` | 启动脚本、定时同步脚本、交易脚本 | 是 |
| `notebook/` | Jupyter 示例、回测实验、画图演示 | 否 |
| `joinquant/` | 聚宽相关样例与数据脚本 | 否 |
| `cookbook/` | 项目文档站素材与说明文档 | 否 |
| `package/` | 本地依赖包，如 `ta-lib`、`pytdx` wheel | 否 |
| `README.md` | 项目介绍、能力展示、外部链接 | 入门必读 |
| `pyproject.toml` | Python 依赖声明 | 重要 |
| `setup.py` | 旧式打包配置，偏轻量 | 次要 |
| `windows_*.bat` | Windows 下安装/启动辅助脚本 | 次要 |

### 可以先记住这句

真正决定项目怎么跑、怎么算、怎么展示的，主要是 `src`、`web`、`script`。  
`notebook`、`joinquant`、`cookbook` 更像样例、说明书和资料库。📚

## 3. 核心代码主干

### 3.1 `src/chanlun`：项目真正的核心

这个目录是主引擎，负责缠论数据处理、市场接入、策略、回测、实盘、选股等。

可按职责理解为：

- `cl.py`
  - 核心缠论计算主逻辑
  - 如果你想知道“缠论对象是怎么逐 Bar 计算出来的”，这里必须看
- `cl_interface.py`
  - 缠论接口层/抽象层
  - 有助于先理解对外暴露了哪些主要对象和能力
- `cl_analyse.py`
  - 偏分析与辅助判断逻辑
- `base.py`
  - 通用基础数据结构、枚举、公共对象
- `config.py.demo`
  - 配置模板，包含 Web、数据库、Redis、交易所接入、账号参数等
- `db.py` / `file_db.py` / `rd.py`
  - 数据库存储、文件存储、缓存相关
- `kcharts.py`
  - K 线图表相关处理
- `monitor.py`
  - 监控任务相关
- `zixuan.py`
  - 自选组相关功能

### 3.2 `src/chanlun/exchange`：市场与数据源接入层

如果你想知道“这个项目是怎么拿到行情数据的”，重点看这里。

目录下按市场/数据源拆了很多实现，例如：

- `exchange_tdx.py`：A 股通达信
- `exchange_tdx_hk.py`：港股通达信
- `exchange_tdx_us.py`：美股通达信
- `exchange_binance.py`：数字货币合约
- `exchange_binance_spot.py`：数字货币现货
- `exchange_futu.py`：富途
- `exchange_ib.py`：盈透
- `exchange_alpaca.py` / `exchange_polygon.py`：美股接口
- `exchange_ctp.py` / `exchange_tq.py`：期货相关
- `exchange_db.py`：从数据库读取行情
- `exchange.py`
  - 统一入口，通常用于按市场拿具体交易所实现

如果你要新增一个数据源，通常是从 `exchange` 目录开始。

### 3.3 `src/chanlun/strategy`：策略层

这里放策略实现和策略示例，文件名基本能直接看出策略意图，例如：

- `strategy_demo.py`：演示策略
- `strategy_xd_mmd.py`：线段买卖点策略
- `strategy_bc.py`：背驰相关策略
- `strategy_last_zs_3mmd.py`：中枢/三买卖点相关策略

如果你想“改信号逻辑”或“加自己的策略”，一般先从这里下手。

### 3.4 `src/chanlun/backtesting`：回测层

这个目录负责把策略和历史数据串起来做回测，例如：

- `backtest.py`：回测核心
- `backtest_trader.py`：回测交易过程
- `backtest_klines.py`：回测 K 线组织
- `optimize.py`：参数优化
- `signal_to_trade.py`：信号转交易动作

如果你想看“策略是如何被跑起来并统计结果的”，这里是关键入口。

### 3.5 `src/chanlun/trader`：实盘/交易执行层

这里是不同市场的交易执行封装，例如：

- `trader_a_stock.py`
- `trader_hk_stock.py`
- `trader_futures.py`
- `trader_currency.py`
- `trader_ctp.py`

如果策略已经有了，接下来要接实盘，就会走到这里。

### 3.6 `src/chanlun/xuangu`：选股层

这个目录用于选股逻辑与批量筛选相关功能。

### 3.7 其他接入层目录

`src` 下除了 `chanlun`，还有 3 个外部框架适配层：

- `src/cl_myquant`
  - 掘金量化接入
  - `run.py` 是典型运行入口
- `src/cl_vnpy`
  - vn.py 接入
  - `run_app.py`、`run_bt.py` 分别偏应用和回测
- `src/cl_wtpy`
  - wtpy 接入
  - `run_bt.py` 是一个典型回测入口

理解方式很简单：  
`src/chanlun` 是“缠论核心”，`cl_myquant` / `cl_vnpy` / `cl_wtpy` 是“把核心能力接到外部量化平台上”。

## 4. Web 相关代码在哪？

### 4.1 Web 主入口

Web 入口在：

- `web/chanlun_chart/app.py`

它做的事情很直接：

- 把 `src` 加到 `sys.path`
- 初始化 Flask 应用
- 用 Tornado 包一层跑 HTTP 服务
- 默认启动后打开浏览器

如果你只想知道“Web 是从哪里启动的”，先看这个文件。

### 4.2 Flask 应用主体

更核心的 Web 逻辑在：

- `web/chanlun_chart/cl_app/__init__.py`

这个文件很重要，因为它同时负责：

- Flask 应用初始化
- 登录逻辑
- 定时任务调度器初始化
- 页面路由
- TradingView `/tv/*` 数据接口
- 图表保存、模板保存、标记接口

如果你想改 Web 行为，不要只看模板，先看这个文件。

### 4.3 页面资源

静态文件和模板都在这里：

- `web/chanlun_chart/cl_app/templates/`
- `web/chanlun_chart/cl_app/static/`

常见关注点：

- `templates/index.html`
  - 主图表页面模板
- `static/js/charts.js`
  - TradingView 图表初始化、图形绘制、标记处理
- `static/charting_library/`
  - TradingView Advanced Charts 静态资源
- `static/datafeeds/udf/`
  - UDF datafeed 前端实现

## 5. 运行脚本在哪？

### 5.1 `script/crontab`

这里主要是定时同步脚本和批处理任务，例如：

- A 股、港股、美股、期货、数字货币 K 线同步
- 历史选股任务
- IB 相关任务

如果你要改“定时跑什么”，主要看这里。

### 5.2 `script/trader`

这里主要是不同市场的交易重启脚本，例如：

- `reboot_trader_a_stock.py`
- `reboot_trader_ctp.py`
- `reboot_trader_currency.py`
- `reboot_trader_futures.py`
- `reboot_trader_hk_stock.py`

如果你要看某个交易任务是怎么独立拉起的，从这里进入。

### 5.3 `script/*.config.js`

这些配置更偏运行/进程管理辅助，不是业务核心，但部署时可能会用到。

## 6. 哪些目录是示例和资料？

### `notebook/`

主要是 Jupyter Notebook 示例，例如：

- 图表展示
- 回测分析
- 数据回放
- 参数优化

适合学习和实验，不适合当作主业务入口。

### `joinquant/`

聚宽相关样例和脚本，偏平台适配示例。

### `cookbook/`

项目文档站素材。  
如果你要找安装文档、配置说明、策略说明、FAQ，这里很有用；但如果你要改业务逻辑，优先还是看 `src` 和 `web`。

### `package/`

这里放本地依赖 wheel 包，例如 `ta-lib`、`pytdx`。  
通常不要直接改这里，除非你明确在处理依赖安装问题。

## 7. 新手推荐阅读顺序

如果你是第一次接触，建议按这个顺序读：

1. `README.md`
   - 先知道项目做什么、能支持哪些市场、有哪些功能
2. `src/chanlun/config.py.demo`
   - 先知道系统依赖什么配置
3. `src/chanlun/cl.py`、`cl_interface.py`、`cl_analyse.py`
   - 了解核心缠论计算主干
4. `src/chanlun/exchange/`
   - 了解行情从哪里来
5. `web/chanlun_chart/app.py` + `web/chanlun_chart/cl_app/__init__.py`
   - 了解 Web 如何启动、如何提供 `/tv/*` 数据
6. `web/chanlun_chart/cl_app/static/js/charts.js`
   - 了解前端图表是如何初始化和画标记的
7. `src/chanlun/strategy/`、`backtesting/`、`trader/`
   - 再按你的目标去看策略、回测或实盘

## 8. 如果你想改某类功能，应该去哪？

| 目标 | 优先看哪里 |
| --- | --- |
| 看缠论核心算法 | `src/chanlun/cl.py`、`cl_interface.py`、`cl_analyse.py` |
| 改配置项 | `src/chanlun/config.py.demo` |
| 加新市场或新数据源 | `src/chanlun/exchange/` |
| 改策略逻辑 | `src/chanlun/strategy/` |
| 改回测逻辑 | `src/chanlun/backtesting/` |
| 改实盘执行 | `src/chanlun/trader/` |
| 改选股逻辑 | `src/chanlun/xuangu/` |
| 改 Web 接口 | `web/chanlun_chart/cl_app/__init__.py` |
| 改 TradingView 图表行为 | `web/chanlun_chart/cl_app/static/js/charts.js` |
| 改页面模板 | `web/chanlun_chart/cl_app/templates/` |
| 改定时任务 | `script/crontab/` |

## 9. 新人常见误区

### 不建议一上来就改这些地方

- `notebook/`
  - 它更像实验记录，不是主系统入口
- `cookbook/docs/img/`
  - 纯图片素材
- `package/`
  - 依赖包，不是业务代码

### 更推荐先看这些地方

- `README.md`
- `src/chanlun/config.py.demo`
- `src/chanlun/`
- `web/chanlun_chart/`

### 一个判断标准

如果你改的是“系统如何计算、如何取数、如何展示、如何交易”，优先看 `src`、`web`、`script`。  
如果你改的是“说明文档、示例图、实验笔记”，才去看 `cookbook`、`notebook`、`joinquant`。

## 10. `charting-library` 在其他项目复用，应该怎么做？

这是很多人第一次接触时最容易误判的地方：

> 当前项目里的 `charting-library` 不是一个独立前端项目，  
> 而是“静态资源 + 前端初始化代码 + UDF datafeed + 后端 `/tv/*` 接口”组合在一起使用的。

所以，**不能只复制 `charting_library` 目录就指望直接跑起来**。⚠️

### 10.1 当前项目里相关文件在哪里？

最关键的几块如下：

- TradingView 静态资源
  - `web/chanlun_chart/cl_app/static/charting_library/`
- UDF datafeed 前端代码
  - `web/chanlun_chart/cl_app/static/datafeeds/udf/`
- 页面模板参考
  - `web/chanlun_chart/cl_app/templates/index.html`
- 图表初始化与自定义绘图逻辑
  - `web/chanlun_chart/cl_app/static/js/charts.js`
- 后端数据接口
  - `web/chanlun_chart/cl_app/__init__.py` 下的 `/tv/*`

### 10.2 最小迁移集：只让基础图表跑起来

如果你的目标只是：

- 能显示 K 线
- 能切换品种/周期
- 能从你自己的后端拿数据

那么至少要带走这些内容：

1. `static/charting_library/`
2. `static/datafeeds/udf/dist/bundle.js`
3. 一份图表容器页面
4. 一段 `TradingView.widget` 初始化代码
5. 一个能响应 UDF 协议的后端 `/tv/*` 接口

### 10.3 完整迁移集：尽量复刻当前项目能力

如果你还希望保留这些能力：

- 图表布局保存
- 指标模板保存
- drawings 保存
- 缠论 marks / timescale marks
- 自定义图形标注

那除了最小迁移集，还要继续迁：

- `charts.js` 里的自定义绘图与交互逻辑
- `/tv/<version>/charts`
- `/tv/<version>/study_templates`
- `/tv/<version>/drawings`
- `/tv/marks`
- `/tv/timescale_marks`
- `/tv/del_marks`

### 10.4 推荐迁移步骤

#### 第一步：复制静态资源

先把下面两个资源放到你的新项目静态目录中：

- `charting_library/`
- `datafeeds/udf/dist/bundle.js`

建议保持目录结构尽量接近，例如：

```text
your-project/
  static/
    charting_library/
    datafeeds/
      udf/
        dist/
          bundle.js
```

#### 第二步：在页面中引入脚本

参考当前项目的 `templates/index.html`，至少要引入：

```html
<script src="/static/charting_library/charting_library.standalone.js"></script>
<script src="/static/datafeeds/udf/dist/bundle.js"></script>
```

然后页面里准备一个容器：

```html
<div id="tv_chart_container"></div>
```

#### 第三步：初始化 TradingView 图表

可以先按当前项目的模式跑最小版本：

```html
<script>
  const datafeed = new Datafeeds.UDFCompatibleDatafeed("/tv", 30000);

  const widget = new TradingView.widget({
    container: "tv_chart_container",
    autosize: true,
    symbol: "a:SH.000001",
    interval: "30",
    datafeed: datafeed,
    library_path: "/static/charting_library/",
    timezone: "Asia/Shanghai",
    locale: "zh",
    theme: "Light",
  });
</script>
```

这里最关键的是两点：

- `library_path` 必须能正确指向 `charting_library/`
- `new Datafeeds.UDFCompatibleDatafeed("/tv", 30000)` 说明前端默认会请求你的 `/tv/*` 接口

如果你想换接口前缀，也可以改成：

```js
new Datafeeds.UDFCompatibleDatafeed("/your-tv-api", 30000);
```

但后端接口也要一起改。

#### 第四步：后端补齐最基础的 `/tv/*` 接口

要让基础图表跑起来，后端最少要有这些接口：

- `/tv/config`
- `/tv/symbols`
- `/tv/search`
- `/tv/history`
- `/tv/time`

当前项目这些接口都定义在：

- `web/chanlun_chart/cl_app/__init__.py`

如果你是迁到 Flask、FastAPI、Django、Node 等别的项目，核心不是框架，而是：

- 返回格式要兼容 UDF datafeed
- `history` 要能按 symbol、resolution、时间范围返回 K 线
- `symbols` / `search` 要能让 TradingView 识别你的标的

#### 第五步：需要保存图表布局时，再补存储接口

如果你只想看图，不需要保存图表布局，这一步可以先不做。

如果你需要保存图表和模板，再补这些接口：

- `/tv/<version>/charts`
- `/tv/<version>/study_templates`
- `/tv/<version>/drawings`

当前项目里，这些接口已经接好了图表内容持久化逻辑。

#### 第六步：需要缠论标记时，再补 marks 接口

如果你只想显示普通 K 线，这一步可以不做。

如果你还想复用当前项目的缠论标记能力，再继续看：

- `/tv/marks`
- `/tv/timescale_marks`
- `/tv/del_marks`
- `static/js/charts.js` 中的绘图和标记逻辑

这部分已经不是“单纯接入 TradingView”，而是“把本项目的缠论标注系统一起迁过去”。

### 10.5 迁移时最容易踩的坑

#### 坑 1：只复制 `charting_library`

这通常会导致页面能加载脚本，但图表拿不到数据，最终空白或报错。

#### 坑 2：`library_path` 不对

`library_path` 必须指向静态目录中的 `charting_library/`，而且尾部通常保留 `/` 更稳妥。

#### 坑 3：后端接口有了，但返回格式不兼容

`/tv/history`、`/tv/symbols`、`/tv/search` 这些接口不是“随便返回 JSON”就行，必须兼容 UDF datafeed 期待的格式。

#### 坑 4：把“基础图表接入”和“完整功能迁移”混为一谈

最小接入只需要：

- 静态资源
- 初始化代码
- 基础 `/tv/*` 数据接口

完整迁移才需要：

- 保存布局
- 保存模板
- drawings
- marks
- 自定义绘图系统

### 10.6 最实用的迁移建议

如果你的目标是尽快在别的项目里用起来，建议按这个顺序：

1. 先迁静态资源 + 最小页面 + `/tv/history`
2. 确认基础 K 线能显示
3. 再补 `symbols`、`search`、`config`、`time`
4. 最后再考虑布局保存、模板保存、marks、自定义标注

这样最稳，也最容易定位问题。✅

## 11. 一句话总结

这个项目最值得先抓住的主线是：

- `src/chanlun` 负责“算什么”
- `exchange` 负责“数据从哪来”
- `strategy/backtesting/trader` 负责“怎么用这些信号”
- `web/chanlun_chart` 负责“怎么把结果展示出来”

而 `charting-library` 想迁到别的项目时，记住一句话就够了：

> 迁的不只是静态库，还包括 datafeed 和 `/tv/*` 后端接口。
